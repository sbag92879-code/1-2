1.
wrap 키거나 다른 WIFI접속시 네트워크 다르다

(네트워크가 동일해야 한다)
heidSQL 프로그램으로 각 pc에 접속가능하다

2. 스키마 (데이터베이스)
create database 데이터베이스명;
create database phm;

3. 테이블 생성
create table aa0{
    title varchar(200) null,
    content varchar(200) null
};

4. 테이블안에 행삽입
insert into aa0 (title, content) values ('안녕', 'mysql');

5. 테이블 행삭제
DELETE FROM `pmh`.`aa0` WHERE  `title`='hi' AND `content`=' ' LIMIT 1;

6. 테이블 행수정
update `pmh`.

7. 테이블 모든 행 조회
SELECT * FROM `pmh`.`aa0`;